# DNDMobile MAUI

This directory contains a .NET MAUI remake of the Expo starter UI.

## Requirements

- .NET 8 SDK with the MAUI workload installed (`dotnet workload install maui`).

## Run

```bash
cd maui

dotnet build
```

Then use your platform-specific tooling (Android emulator, iOS simulator, or Windows) to run the app.
